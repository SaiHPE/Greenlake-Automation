# Findings register — rack13arcus live run, 2026-09-12/13 (v0.16.0-rc.5 → rc.7)

Everything found while an operator drove Discovery → Zoning → Provisioning through the shipped UI
against real hardware, plus a code audit of the frontend against [UX-GUIDELINES.md](../UX-GUIDELINES.md).
One line per finding; the validation record holds the raw evidence
([2026-09-12-rack13arcus-live-test-2.md](../validation/2026-09-12-rack13arcus-live-test-2.md)).

**Evidence tags** — *live*: observed on hardware or in the operator's UI today · *code*: verified by
reading the source · *shot*: needs a screenshot to confirm (listed at the end).
**Severity** — **D** defect (wrong answer or lie) · **G** gap (customer case not covered) ·
**U** UX (right answer, hard to read/act on) · **P** polish.

## Status of earlier findings

| # | Finding | Now |
|---|---|---|
| Z8 | Zoning host list vCenter-only | fixed rc.6, **confirmed live** (`arcus-win137`, `localhost.localdomain` appear) |
| — | Continue hidden when only some hosts pass the gate | fixed rc.7, **confirmed live** |
| "defect 4" | VV-set "Created" but absent | **withdrawn** — operator ran the cleanup commands before the check; the array's event log for run 2 shows `createvvset zz_t2_vvs zz_t2_vol01 zz_t2_vol02` and `createvlun set:zz_t2_vvs 0 set:zz_t2_hs`, exactly right. Superseded by P-1 below. |

## Defects

| ID | Step | Finding | Evidence | Fix |
|---|---|---|---|---|
| **P-1** | Provision | **Export rows never carry an existence check.** `build_plan` sets `exists` on host/hostset/volume/vvset rows but not on `vlun` rows, and the WSAPI client has no VLUN read. Every export shows *Create* on every rebuild; "8 to create · 0 already exist" is wrong on a re-run; the footer's "re-running skips existing objects" is not honoured in the plan. Apply is protected only by the array returning a conflict. | live (Rebuild plan after apply: 2 vlun rows *Create* while LUNs 0/1/2 existed); code `storage_provision.py` `for ex in exports` | Add `vluns_for(volume)` to the client (`GET /vluns` filtered); `exists` = every member volume of the source has a template to the target (that is how `showvlun -t` records a set export); after `ensure_vlun` returns `created`, read it back once. |
| **P-2** | Provision | **Provisioning host list is vCenter-only.** `_hosts_by_name`/`_persona_by_host` read `discovery.host_hbas`. The Compose dropdown offered `.136/.47/.86`; `arcus-win137` (sheet) and `localhost.localdomain` (fabric) — which the zoning step now lists — cannot be provisioned. | live (dropdown screenshot); code | Same union as zoning rc.6: vCenter ∪ sheet ∪ array logins ∪ local NS, with source tag; persona from OS where known (`windows` → WindowsServer, `linux` → Generic-ALUA). |
| **P-3** | Provision | Compose dropdown for an **FC host set** offers **iSCSI-only existing hosts** (`HPE_VM_*`, `grp3_vmenode*`) as members. Picking one produces a set whose member can never see an FC export. | live (dropdown screenshot) | Filter member options by transport of the intent (FC unless the volume export is iSCSI); or label them "iSCSI — not eligible for FC export" and disable. |
| **P-4** | Provision | The vvset **existence check may miss** — unproven. On the operator's rebuild the vvset row read *Create*; at that moment the set had been removed by hand, so the row was probably right. | live, ambiguous | Rebuild plan once with the set present (id 33). If *Exists*, close. If *Create*, `volume_set_names()` is paging/filtering wrong. |
| **D-1** | Discovery | **Parity/cabling notes not visible.** Code emits "both switches attach at the same card-port parity" + two mismatch notes for this cross-cabled array; the operator's Discovery paste shows none. | live (paste), code `discovery.py:_refine_fabrics_from_switches` | Confirm whether they land in the "Discovery notes" notification or are dropped; either way, show them beside the port table (they now also reach the zoning plan). |
| **Z-A** | Zoning | Fabric-only initiators (on a switch, zoned to nothing) appear in **Zoning** but not in **Discovery**, because Discovery reads array + vCenter only. For a greenfield customer the "hosts that need zones" list is *the* host list and it's on the wrong screen. | live | Discovery reads the declared switches' local NS too (already read for the plan) and shows "on fabric, not zoned to this array" as a host state. |

## Gaps (scope, not bugs)

| ID | Finding | Evidence |
|---|---|---|
| **G-1** | No tool-generated zone has yet been **applied and seen to log a host in**. The single most important unproven link. Candidate: `localhost.localdomain` on F1 (`mycfg`, training team's own switch, no aliases). | live |
| **G-2** | Windows/Linux hosts not in vCenter: only sheet-declared (WWPNs typed by hand) or NS-named. No agentless login (WinRM/SSH) to read OS, WWPN, multipathing. SCOPE.md area 2, priority 2. | code |
| **G-3** | ESXi-side verification stops at the array. `esxcli storage core path list` would prove the 12 paths the customer cares about. | design |

## UX — per step

### Discovery *(live paste + code)*
| ID | Finding | Sev |
|---|---|---|
| D-2 | "Unidentified hosts" lists `vmenode`, which **is** a named array host object — misfiled. | U |
| D-3 | RCIP ports read "Available — cabled and capable" while Link is *offline*. Contradiction in one row. | U |
| D-4 | "5 name(s) free on this array" counts volumes/sets but not the hosts the run will create. | P |
| D-5 | Eleven iSCSI hosts and six FC unclaimed logins are shown at the same weight as the three hosts the run is about; no "in this run / not in this run" grouping. | U |
| D-6 | Section titles use the array's vocabulary ("Hosts known to the array", "Unclaimed logins") with an explanatory paragraph each; a legend + consistent status column would carry it. | U |

### Zoning *(live: two rounds of screenshots)*
Already redesigned in rc.6 (panels A–F). Left open:
| ID | Finding | Sev |
|---|---|---|
| Z-1 | Legend for grey ✓ (exists) vs green ✓ (selected) still implicit in the designer. | U |
| Z-2 | Command set is per fabric but there is no "which switch do I paste this into" line at the top of the paste block itself (only in the panel header). Put the `# switch 10.132.30.111 …` header inside the copied text. | U |
| Z-3 | Alias inputs take free text; the FOS validator flags but does not *offer* a corrected name. | P |
| Z-4 | Activation block shows `cfgsave` then `cfgenable`; the FOS doc order is `cfgtransshow` → paste → `cfgsave` → `cfgenable`. `cfgtransshow` is a sentence, not a line in the block. Make it the first line. | P |
| Z-5 | Left action reads "Check zoning" until a report exists; the operator did not know that the button he needed lived behind it (two screenshots, one live confusion). Rename to "Check zoning on the array" and make the Continue affordance's absence explain itself ("Check zoning to see which hosts can be provisioned"). | U |

### Provision *(live)*
| ID | Finding | Sev |
|---|---|---|
| P-5 | Compose card **resets to "Load objects" after apply**; saved membership/exports vanish from view although they were used. Auto-load on mount; keep the composition visible after apply, read-only. | U |
| P-6 | "Leave it untouched to use the default (every volume → every host-set)" is stated twice and describes a default the code *refuses* when there is more than one host set. Say exactly what will happen. | U |
| P-7 | Plan "Action" column uses `not_started` styling for *Exists* — the DS status vocabulary (Not started) doesn't mean "already there". Needs its own indicator (e.g. neutral check "Exists"). | U |
| P-8 | Result "Detail" is `—` for every row. Fill it with the array's identifier (VV id/WWN, host id, LUN ids) — the event log has them and so does the WSAPI response. | U |
| P-9 | Path verification says "2 HBA(s) live on both fabrics" but not *how many paths per LUN* (4) or which LUN ids. The number the customer asks for is missing. | U |
| P-10 | "Continue" is always enabled, including before anything was created. Correct per the code comment, but the label should say what continuing means ("Continue without provisioning" when nothing was applied). | P |

### Verify / As-built *(live output paste, code)*
| ID | Finding | Sev |
|---|---|---|
| V-1 | `checkhealth` issues render as flat Component / Id / Description / *Manual* rows. Group by component, count per severity, and mark which are pre-existing vs new since the baseline (the as-built has both timestamps). | U |
| V-2 | Every issue is tagged "Manual" with no explanation of what Manual means or what the alternative is. | U |
| V-3 | The word **"health"** survives in code (`report.health_issues`) and in the CLI command shown to the operator (`checkhealth`). UI title is already "Array status" — keep it that way in every new string. | P |
| A-1 | As-built: needs a screenshot/document review — what the VV-set and export sections show for this run (the set exists; do they render it?). | shot |

### Cross-cutting *(code audit vs UX-GUIDELINES)*
| ID | Finding | Sev |
|---|---|---|
| X-1 | Hard-coded pixel widths in `ProvisioningBuilderView` (5), `ZoningPlanView` (4), `ModeStep` (2) — DS says t-shirt tokens only. | P |
| X-2 | Two spinners possible in the builder card at once. | P |
| X-3 | `a11yTitle`/`aria-*` present in only 4 files; DataTables have no `aria-describedby` to their summaries. | P |
| X-4 | Eight steps end with a bare **Continue**; DS asks for verb + noun that says what happens ("Continue to Provision storage"). `StepShell` knows the next step's title. | U |
| X-5 | Empty-state texts vary in quality; DS wants *what's missing, why, the way forward* in each. Zoning's is good now; Discovery's "Nothing discovered yet" and Provision's "Compose the objects below" are not. | P |
| X-6 | Long single-line notifications joined with " · " (Discovery notes, Plan notes, Check notes) — the DS anatomy is one message per notification or a list. | U |
| X-7 | No "what changed since last time" anywhere except the new zoning re-check diff. Discovery and Provision would benefit from the same pattern (the run store already keeps every event). | U |

## Screenshots that would close the open items

1. **Discovery** — the "Discovery notes" notification (bottom of the page) and the Array target ports table together: closes D-1.
2. **Provision** — Rebuild plan now, with `zz_t2_vvs` present: the three rows vvset / vlun / vlun. Closes P-4.
3. **As-built** — the generated `.docx` sections for volumes, VV-sets, hosts, host sets, exports (or the file itself). Closes A-1 and tells us whether the document shows the set export as one line or two.
4. **Zoning** — the top of the copied paste block as pasted into Notepad (not the UI): confirms Z-2.
5. **Verify** — the configuration comparison table (sheet vs array), any row flagged as a mismatch: gateway was inferred in the sheet, so a false mismatch there is expected and tells us how the mismatch is worded.

No further scripts are needed for the catalogue. One optional array command for the record's completeness: `showeventlog -min 90 -oneline -msg zz_t2` would show run 1's `createvvset` at 00:01 and the manual `removevvset` that followed — it closes the run-1 story with the array's own words rather than inference.

## Suggested order

1. **P-1** (existence check + read-back) — the plan is the approval document; it must be true. Small, testable against today's captures.
2. **P-2** (host union for provisioning) — same code shape as the zoning fix; unlocks the Windows/Linux customer case end to end.
3. **P-5, Z-5, X-4** — the three things that made the operator stop and ask "where is the button / where did my work go" today.
4. **G-1** — the zoning apply on `localhost.localdomain`, F1. Needs the training team's nod for `cfgenable mycfg`.
5. Everything else, in the order of the tables above.
